{
  "name": "server",
  "version": "1.0.0",
  "description": "GraphQL Apollo Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch db.json",
    "node:server": "nodemon --exec babel-node index.js",
    "start": "concurrently --kill-others \"npm run json:server\" \"npm run node:server\""
  },
  "keywords": [],
  "author": "Dusan Brnic",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.10.0",
    "axios": "^0.19.2",
    "graphql": "^14.6.0",
    "json-server": "^0.15.1"
  },
  "devDependencies": {
    "@babel/core": "^7.8.4",
    "@babel/node": "^7.8.4",
    "@babel/preset-env": "^7.8.4",
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.2"
  }
}
